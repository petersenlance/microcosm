setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    MIX_ENV: prod
release:
  image: web
  command:
    - echo Hello to the world
run:
  web: _build/prod/rel/microcosm/bin/microcosm start
