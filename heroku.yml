setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    MIX_ENV: prod
release:
  image: web
  command:
    - MIX_ENV=prod mix ecto.migrate
run:
  web: iex -S mix phx.server
